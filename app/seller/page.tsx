import SellerPortalClient from '@/components/SellerPortalClient'

export default function SellerPage() {
  // Simplified: Let client handle authentication and data fetching for faster loading
  return <SellerPortalClient />
}

